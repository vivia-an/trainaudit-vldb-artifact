# 06 · 待办与开放问题

## 优先级 P0（必须做，否则故事讲不动）

### P0-1：Rule Deficit 的硬数据证据（证据 1）
- **任务**：在 Megatron-LM 仓库 grep `assert`、`raise *Error`、`if not … : raise`，人工筛掉与训练状态无关的（如用户输入校验、文件路径检查），保留与"训练状态/分布式状态/optimizer state"相关的；
- **目标产物**：一个数字 N₁，预期 30–80 之间；一份分类清单（按"DP / TP / PP / 优化器 / 数值"分类）放进 Appendix；
- **工时**：半天到一天；
- **影响位置**：§1 第一段、§2.3 新增小节、Appendix；
- **谁做**：开发者本人最熟悉代码，建议不要外包。

### P0-2：249 条 invariant 中能被 TrainCheck template 表达的比例（证据 2）
- **任务**：人工分类 249 条 invariant，每条标注是否能用 `Consistent / EventContain / APISequence / APIArg / APIOutput` 中任一模板表达；
- **目标产物**：一个比例（预期 80%+ 不可表达）；每类各 3 个例子；
- **工时**：1 天；
- **影响位置**：§4 RQ1、§5 对比表、Appendix；
- **关键判断标准**：要严格——只要一个 template 能勉强表达就算可表达。这样数字才站得住。

### P0-3：Proposition 1 的反例 trace dump（证据 3）
- **任务**：把 B10（micro_step_id）的正确执行 trace 和错误执行 trace 都打印出来；
- **目标产物**：一个 figure/table，显示两次执行的 API call 序列在 token 级别完全相同；
- **工时**：半天；
- **影响位置**：§2.2 Proposition 1 之后；
- **风险**：要选对 case。B10 的正确/错误执行是否真的 trace 相同？需要先验证再写。

## 优先级 P1（强烈建议做）

### P1-1：核心超参敏感性表（应对原 review 中的 M4）
- **任务**：在 30 个抽样 constraint 上跑 $\theta \in \{0.6, 0.7, 0.8, 0.9\}$ 的 mining ablation，记录 accept rate / 后续 detection rate / FPR；
- **目标产物**：一张 4×3 表格放在 Appendix；正文一段两句话指向；
- **工时**：1 天 + ~\$50 API 成本；
- **影响位置**：Appendix + §3.2 一句"sensitivity analysis confirms robustness in $[0.7, 0.9]$"。

### P1-2：人工 vs AI 的 invariant 成本对比
- **任务**：估算"专家手写一条与 TrainAudit 同等粒度的 invariant"的时间和成本；可以通过对若干 invariant 做计时实验；
- **目标产物**：一个数字（如 \$20–50/invariant）和一句方法说明；
- **工时**：半天；
- **影响位置**：§3.2 Mining Cost 段落；
- **替代方案**：如果不方便做计时，引用一篇 software engineering 文献中关于"专家撰写 spec 的时间成本"的估算。

### P1-3：Cross-framework 部分的"降级或加强"决策
- **决策点**：选保守（§4.4 改名为 Structural Analysis、去掉具体百分比）还是积极（补真实 DeepSpeed 注入实验）；
- **保守版工时**：半天改写；
- **积极版工时**：5–10 天（搭 DeepSpeed 注入环境）；
- **建议**：先做保守版，把工时省下来攻 P0；如果 reviewer rebuttal 阶段有空再补真实实验。

## 优先级 P2（锦上添花）

### P2-1：B11–B15 的 20-repetition 补充
- **任务**：现有 20-rep 实验只覆盖 B1–B10；补 B11–B15 的同样实验；
- **工时**：1–2 天；
- **影响位置**：§4.1 Statistical Analysis、Appendix Table 11；
- **重要性**：如果 B11–B15 在 20 次重跑中显示更高方差，这是诚实数据；如果稳定，是更强证据。

### P2-2：LLM-generated SQL 失败率统计
- **任务**：在 33 个 injection scenario 的 3,150 次 constraint evaluation 中，统计 fallback regex parser 触发的次数；
- **工时**：半天（看现有 log 即可）；
- **影响位置**：§3.4 Resilient Parsing 段落，加一句具体数字；
- **影响**：堵住"你们靠 LLM 生 SQL，靠谱吗"这个隐性质疑。

### P2-3：2-node 多机 overhead 验证
- **任务**：在 2-node 16-GPU 上跑同样的训练 + collector + verifier，测量 overhead 是否仍然 ~5%；
- **工时**：3–5 天（含环境搭建）；
- **影响位置**：§4.3；
- **建议**：低优先级——如果时间紧张可以放 limitations 列出"single-node evaluation"；reviewer 确实问起再补。

## 写作侧的待办

### W-1：避开 CLAUDE.md 列出的禁词
- 已 grep 确认当前版本不含 `delve / leverage / underscore / pivotal / crucial / notable / seamlessly`；
- 重写 Abstract 和 Introduction 时**继续避开**这组词；
- 同时避开 "it is worth noting that" 和 "in this paper, we propose" 这种套路开头。

### W-2：修文本笔误与 label 位置
- `main.tex:303` 删除 "Our approach." 残骸；
- `main.tex:348` 的 `\label{subsec:invariant_miner}` 移到 `main.tex:388` 的 `\subsection{Invariant Miner}` 紧后；
- `main.tex:349-366` 的 Tab. 1 物理位置移到 §3.2 附近（不只是逻辑上属于 §3.2）；
- Eq. (3) 改用 `\text{Valid}`、`\text{Error}`；
- 全文 "Naïve / Naive" 统一一种写法。

### W-3：Tab. 1 与 Definition 6.1 的 agent set 不一致
- Tab. 1 列了 5 个 agent（含 Coordinator），Definition 6.1 的 $A$ 只列 4 个；
- 决定 Coordinate Agent 是否属于 FSM 的一部分，并在两处统一表述；
- 建议在 Tab. 1 caption 里加注 "Coordinate Agent does not occupy a state; it routes transitions"。

## 引用核查（高风险，必须本人核对）

CLAUDE.md 已强调 AI 生成的引用约 30% 有错。重点核查：
- **`ttrace2025`**：作者、年份、出处、bibtex 是否一致；
- **`trainverify2025`**：同上；
- **`jiang2025traincheck`**：OSDI 2025 的接收信息、是否真的报告 18/20 在自家 benchmark 上；
- **`pei2023can`**：论文是否真的讨论"invariant generation from programs"；
- **`pandita2012inferring`**：作者 / 年份 / 标题；
- **`grant2018inferring`**：同上。

每条都要回到原 paper 验证三件事：① 论文确实存在 ② 作者/年份/标题与 main.bib 一致 ③ 引用的具体结论在原文中可查。

## 故事级开放问题（讨论中）

### Q1：Abstract 第一句到底用故事 A 还是故事 B 当钩子？
- 故事 A（"信息一直都在"）更稳，但 less catchy；
- 故事 B（"问错了问题"）更 catchy，但语气大；
- **当前倾向**：用故事 B 当 Abstract 第一句钩子，进 Introduction 后立刻切换到故事 A 的 rule deficit 框架展开。

### Q2：要不要明确给"AI 时代 debugging 第五时代"这种 claim？
- 故事 C 的"四时代 → 第五时代"框架很有冲击力，但风险高；
- **当前倾向**：只在 §5 末段或 Conclusion 用，且把 "first instantiation" 改为 "an early instantiation"；
- Abstract / Introduction 主体不触碰。

### Q3：Contribution 数量取几条？
- 现有 paper 是 3 条 bullet；
- 重写后大纲列了 4 条（加了 Contribution 4：互补性证据）；
- **当前倾向**：合并 Contribution 3 和 4 → 仍然保持 3 条，但每条信息密度提升。

### Q4：要不要把 paper 的题目改了？
- 现有题目：*TrainAudit: Robust Silent Error Detection for LLM Training*——"Robust" 这个词太工程；
- 候选题目：
    - *TrainAudit: Lifting the Rule Deficit for Silent Error Detection in Distributed LLM Training*（直接打概念词，但长）
    - *TrainAudit: Intent-Anchored Invariant Mining for Distributed LLM Training*（更抽象，更高阶）
    - *Asking the Right Question: Faithfulness-Driven Silent Error Detection in LLM Training*（最大胆，但 abstract 化）
- **当前倾向**：第二个，"Intent-Anchored Invariant Mining"——既保留系统名又植入概念词。

# Offline checks need only duckdb; everything else in this artifact's verification path is
# stdlib. Verified: scripts/check_all.sh, core/run_smoke.py, core/example_verifier.py and
# the verify_* scripts import nothing beyond the standard library plus duckdb.
duckdb>=1.0.0

# pyyaml and pydantic are used by the mining pipeline's config loading, not by the checks.
pyyaml>=6.0
pydantic>=2.0

{
  "meta": {
    "stage": "seed",
    "n_cases": 128,
    "date": "2026-07-16",
    "notes": "Raw operator 'structural_integrity' was used by extractors as an umbrella; per §3.2 (objects playing different roles) it was resolved case-by-case from relation_statement into finer canonical operators (update_effectiveness, state_preservation, sharding_layout_consistency, value_scaling_consistency) during axial coding. support_count counts mutually independent incidents (§4.1); positive_examples may exceed it where evidence shows two case_ids describe the same incident (B13/O-002; D-005/D-023; M-015/M-030)."
  },
  "operator_alias_map": {
    "equality_across_ranks": "equality_across_ranks",
    "invariance_across_config": "reference_equivalence",
    "reference_equivalence": "reference_equivalence",
    "selection_correctness": "reference_equivalence",
    "config_state_consistency": "config_effectiveness",
    "value_scaling_consistency": "value_scaling_consistency",
    "proportionality": "value_scaling_consistency",
    "dtype_preservation": "dtype_preservation",
    "conservation": "conservation",
    "ordering": "ordering",
    "restoration_after_reload": "restoration_after_reload",
    "boundedness": "boundedness",
    "monotonicity": "monotonicity",
    "count_frequency_match": "count_frequency_match",
    "exclusivity": "exclusivity",
    "index_consistency": "index_consistency",
    "gradient_flow": "gradient_flow",
    "determinism": "determinism",
    "state_preservation": "state_preservation",
    "sampling_uniformity": "sampling_uniformity",
    "structural_integrity": "structural_integrity"
  },
  "templates": [
    {
      "template_id": "T01",
      "name": "cross-rank-replica-equality",
      "semantic_obligation": "State that the parallelism recipe declares replicated (parameters, reduced gradients, loss-scale/overflow state, RNG for dropout, derived grad-norm scalars) must be bit/checksum-equal across all ranks of its replication group at the same step.",
      "relation_operator": "equality_across_ranks",
      "semantic_objects": ["replicated training state", "replication group"],
      "schema_predicate": "FORALL x in replicated_state, FORALL r1,r2 in replica_group(x): checksum(x, r1, step) == checksum(x, r2, step)",
      "permitted_topology_guards": ["DP>1", "EP>1 and DP>1", "DP>1 and TP>1"],
      "permitted_preconditions": ["ZeRO-1/2 with reduce_scatter", "gradient accumulation over micro-batches", "MoE gate/expert params under ZeRO or distributed optimizer", "fp16 dynamic loss scaler", "model-parallel RNG initialization"],
      "permitted_phases": ["init", "backward", "optimizer_step"],
      "positive_examples": ["D-003", "D-011", "D-031", "D-035", "M-018", "M-021", "D-NEW-61"],
      "counterexamples": ["TP/EP-sharded tensors that are intentionally different per rank must not be compared as replicas", "Local per-rank gradients inspected before the reduction collective has run"],
      "merge_boundary": "Must not absorb logged-metric reduction consistency (T02): metrics are legitimately rank-local until the explicit reduce, so a merged predicate would flag legal executions (§3.5).",
      "support_count": 7
    },
    {
      "template_id": "T02",
      "name": "logged-metric-reduction-consistency",
      "semantic_obligation": "A metric reported to logs must be the designated cross-rank reduction of the per-rank values, hence identical on every rank, whenever distributed metric logging is enabled.",
      "relation_operator": "equality_across_ranks",
      "semantic_objects": ["logged metric", "cross-rank reduction result"],
      "schema_predicate": "FORALL r1,r2: logged_metric(m, r1, step) == logged_metric(m, r2, step) AND logged_metric(m, r, step) == designated_reduce({local_metric(m, r', step) : r' in world})",
      "permitted_topology_guards": ["DP>1"],
      "permitted_preconditions": ["distributed metric/optimizer-metric collection enabled", "MoE aux-loss/z-loss logging with world_size>1", "reduction operands must live on the collective's device"],
      "permitted_phases": ["forward", "optimizer_step", "any"],
      "positive_examples": ["O-014", "O-NEW-29", "O-NEW-30"],
      "counterexamples": ["Per-rank local metric values differing before the explicit all-reduce point", "Deliberately rank-local diagnostics documented as unreduced"],
      "merge_boundary": "Must not absorb T01: the compared object is a reporting artifact, not training state, and its violation corrupts observability rather than the model (§3.2 different semantic roles).",
      "support_count": 3
    },
    {
      "template_id": "T03",
      "name": "config-invariant-execution-equivalence",
      "semantic_obligation": "Outputs, gradients, and converged loss must be invariant (within tolerance) to performance-only execution configuration: parallel degree, sequence divisibility, compile mode, activation recompute, FSDP wrapping/init pathway, and communication bucket size.",
      "relation_operator": "reference_equivalence",
      "semantic_objects": ["computation output under active configuration", "same computation under reference configuration"],
      "schema_predicate": "max_abs_diff(output(cfg, x, w), output(cfg_ref, x, w)) <= tol, for identical inputs x and weights w, where cfg and cfg_ref differ only in a performance-neutral knob (CP/SP degree, torch.compile on/off, recompute on/off, sharding/wrapping layout, bucket size, meta-vs-direct init)",
      "permitted_topology_guards": ["TP>1", "CP>1", "SP>1", "DP>1", "FSDP", "any"],
      "permitted_preconditions": ["sequence length not divisible by world size", "context/sequence parallel attention", "torch.compile over collectives or FSDP set_ ops", "activation checkpointing combined with FSDP wrapping", "param_init_fn with sync_module_states", "configurable reduce_bucket_size"],
      "permitted_phases": ["init", "forward", "backward"],
      "positive_examples": ["D-004", "D-039", "M-002", "M-009", "M-029", "O-018", "O-027", "O-035", "O-036", "O-037"],
      "grounding_concerns": ["O-036: witness requires a paired reference-init run; root cause not isolated in the evidence"],
      "counterexamples": ["Bitwise differences within numerical tolerance caused by legitimate reduction reordering across configurations", "Loss-curve differences caused by knobs that are documented to change semantics (e.g. different global batch size)"],
      "merge_boundary": "Must not absorb T04 spec-reference-conformance: T03's witness is a paired run of the same code under another config, while T04 needs an external spec/implementation oracle - no single observable signal covers the merged set (§3.4).",
      "support_count": 10
    },
    {
      "template_id": "T04",
      "name": "spec-reference-conformance",
      "semantic_obligation": "A computed quantity (attention output/gradient, normalization formula, routing selection, LR schedule, RoPE numerics) must equal the value mandated by its authoritative specification or reference implementation.",
      "relation_operator": "reference_equivalence",
      "semantic_objects": ["computed value", "authoritative spec or reference-implementation value"],
      "schema_predicate": "|computed(q, x) - spec_reference(q, x)| <= tol, where spec_reference is an analytic formula or an independent reference implementation evaluated on identical inputs/weights",
      "permitted_topology_guards": ["any", "TP>1"],
      "permitted_preconditions": ["MLA/yarn attention scaling", "TE fused attention backward on deep models", "group-limited top-k routing configured", "local vs TE layer spec for ViT", "warmup scheduler backend mapping", "RMSNorm forward", "HF-parity RoPE dtype"],
      "permitted_phases": ["forward", "backward", "optimizer_step"],
      "positive_examples": ["M-022", "M-032", "M-035", "M-NEW-24", "M-NEW-40", "O-017", "O-NEW-1", "O-NEW-64", "OC-NEW-10"],
      "grounding_concerns": ["M-022: violation observable only against an external reference implementation; extractor notes no runtime anomaly is raised", "M-032: evidence gives only 'abnormal loss patterns'; low-confidence extraction"],
      "counterexamples": ["Implementations that intentionally deviate from a reference with documented semantics (e.g. a configured non-standard softmax scale)", "Tolerance-level numeric differences between fused and unfused kernels"],
      "merge_boundary": "Must not absorb T03: comparing against an external oracle is a different observable-signal class than comparing the same code under two configs (§3.4); must not absorb T18, whose reference is the user's own configured value, not an algorithm spec (§3.2).",
      "support_count": 9
    },
    {
      "template_id": "T05",
      "name": "designated-dtype-fidelity",
      "semantic_obligation": "Every tensor must carry its designated dtype at its point of use: gradients accumulate/reduce in the designated high-precision dtype, registered buffers and inputs retain their declared dtype, and recomputed activations match the original dtype.",
      "relation_operator": "dtype_preservation",
      "semantic_objects": ["tensor at use point", "designated dtype"],
      "schema_predicate": "FORALL (t, site) in dtype_constrained_uses: dtype(t, site) == designated_dtype(t, mixed_precision_recipe)  -- e.g. dtype(allreduce_operand)==fp32, dtype(grad at optimizer.step)==fp32, dtype(expert_bias at forward)==fp32, dtype(router_input after jitter)==model_dtype, dtype(recomputed_activation)==dtype(saved_activation)",
      "permitted_topology_guards": ["DP>1", "EP>1", "FSDP", "any"],
      "permitted_preconditions": ["BF16/fp16 mixed precision with fp32 accumulation recipe", "ZeRO-3 coalesced all-gather under autocast", "grad_accum dtype differing from model dtype", "full-precision eval of an FSDP model", "activation-checkpointing recomputation", "large world size bf16 reduction"],
      "permitted_phases": ["forward", "backward", "optimizer_step", "eval", "any"],
      "positive_examples": ["D-028", "D-036", "D-042", "D-045", "D-NEW-62", "M-012", "M-024", "O-004", "O-028", "O-031", "O-034"],
      "counterexamples": ["Intentional low-precision reduction explicitly configured and documented by the recipe", "Autocast-driven downcasts at sites the recipe designates as low-precision"],
      "merge_boundary": "Must not absorb T06 storage-dtype-interpretation: the on-disk mismatch is invisible to any runtime dtype tag and is witnessed only by file-size arithmetic, so no single observable signal covers the merged set (§3.4).",
      "support_count": 11
    },
    {
      "template_id": "T06",
      "name": "storage-dtype-interpretation-match",
      "semantic_obligation": "The dtype used to read an on-disk token dataset must equal the dtype the file was written with, so token ids are not silently reinterpreted.",
      "relation_operator": "dtype_preservation",
      "semantic_objects": ["dataset reader dtype configuration", "on-disk token storage dtype"],
      "schema_predicate": "itemsize(configured_reader_dtype) == file_size_bytes / token_count AND max(observed_token_id) consistent with tokenizer vocab_size (vocab_size > 2^16 implies itemsize >= 4)",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["vocab_size > 65536 with dataset built without explicit reader dtype", "memmap/packed token file loading"],
      "permitted_phases": ["data_loading"],
      "positive_examples": ["O-NEW-38", "OC-NEW-16"],
      "counterexamples": ["Datasets legitimately stored as uint16 for vocabularies below 65536", "Files whose header/metadata declares the dtype and matches the reader"],
      "merge_boundary": "Must not absorb T05: the constrained object is a storage-interpretation config, not a runtime tensor, and its violation never shows in any runtime dtype tag (§3.2, §3.4).",
      "support_count": 2
    },
    {
      "template_id": "T07",
      "name": "gradient-accumulation-conservation",
      "semantic_obligation": "The accumulated gradient must equal the exact sum of all per-micro-batch (per-rank) gradient contributions: starting from a zeroed buffer, each contribution added exactly once, with no foreign bytes (e.g. uninitialized padding) mixed in.",
      "relation_operator": "conservation",
      "semantic_objects": ["gradient accumulation buffer", "per-micro-batch gradient contributions"],
      "schema_predicate": "FORALL p: accum_grad(p, after N micro_steps) == SUM_{i=1..N} grad_i(p), with accum_grad(p, 0) == 0 and padding_region(comm_buffer) == 0",
      "permitted_topology_guards": ["DP>1", "PP>1", "any"],
      "permitted_preconditions": ["ZeRO-1/2/3 with CPU offload", "gradient accumulation steps > 1", "immediate grad-update hooks on pipeline non-last stages", "reduce-scatter partition padding", "accumulation outside no_sync with CPU offloading", "buffer-reuse first-iteration zeroing"],
      "permitted_phases": ["backward"],
      "positive_examples": ["D-012", "D-017", "D-NEW-65", "D-NEW-71", "M-040", "O-029"],
      "counterexamples": ["A configured averaging (not summing) accumulation mode where the divisor is documented", "Gradients legitimately reset to zero at accumulation-boundary steps"],
      "merge_boundary": "Must not absorb T08 scale-factor-consistency: a wrong multiplicative factor is a different relation operator (scaling equality) than a missing/extra/duplicated additive term (§3.1).",
      "support_count": 6
    },
    {
      "template_id": "T08",
      "name": "scale-factor-consistency",
      "semantic_obligation": "Every scaled or normalized quantity in the loss/gradient/metric path must carry exactly the factor mandated by its specification (parallel degree, token/sample count, micro-batch count, loss scale), applied exactly once.",
      "relation_operator": "value_scaling_consistency",
      "semantic_objects": ["scaled quantity (loss, gradient, or accumulated metric)", "specified normalization factor"],
      "schema_predicate": "applied_scale(q, step) == spec_scale(q; dp_size, cp_size, sp_size, num_tokens, num_micro_batches, loss_scale, sample_counts), and the factor is applied exactly once (magnitude_ratio(q, reference) == 1)",
      "permitted_topology_guards": ["DP>1", "EP>1", "TP>1", "CP>1", "any"],
      "permitted_preconditions": ["fp16/bf16 loss scaling", "MoE bucket/aux-loss coefficients", "sequence-parallel gradient averaging", "per-token loss normalization", "incomplete minibatch at DP boundary", "metric accumulation over micro-batches"],
      "permitted_phases": ["forward", "backward", "data_loading"],
      "positive_examples": ["D-013", "D-026", "D-034", "D-037", "D-040", "M-003", "M-004", "M-033", "OC-NEW-42"],
      "counterexamples": ["A documented change of normalization convention (e.g. per-token vs per-sequence loss) reflected consistently in config", "Loss-scale growth/decay behaving per the dynamic loss-scaling policy"],
      "merge_boundary": "Must not absorb T09: T09's object is the derived global-norm/clip metric, not a quantity in the training path; merging would mix distinct object roles (§3.2). Must not absorb T27 init-time statistics (§3.2).",
      "support_count": 9
    },
    {
      "template_id": "T09",
      "name": "gradient-norm-computation-fidelity",
      "semantic_obligation": "The reported/applied global gradient norm and clip coefficient must equal a reference recomputation over the actual gradient tensors, with the correct group divisor and epsilon applied exactly once globally.",
      "relation_operator": "value_scaling_consistency",
      "semantic_objects": ["reported global gradient norm and clip coefficient", "reference recomputation from actual gradients"],
      "schema_predicate": "|reported_grad_norm(step) - norm(actual_grads(step))| / norm(actual_grads(step)) <= tol AND clip_coef(step) == max_norm / (global_norm(step) + eps) with eps added exactly once globally and normalization by dp_size (not world_size)",
      "permitted_topology_guards": ["DP>1", "TP>1", "FSDP"],
      "permitted_preconditions": ["CPU-offload gradient clipping", "precision-aware optimizer gradient source (decoupled_grad)", "DTensor sharded clipping", "TP-enabled norm normalization"],
      "permitted_phases": ["optimizer_step"],
      "positive_examples": ["D-016", "M-NEW-67", "O-032", "O-038"],
      "counterexamples": ["Norm values that differ across steps because gradients genuinely changed", "Configured norm types other than L2 computed per their own spec"],
      "merge_boundary": "Must not absorb T08: the constrained object is a derived monitoring/clipping metric recomputable inside the run, not a training-path tensor scale (§3.2).",
      "support_count": 4
    },
    {
      "template_id": "T10",
      "name": "producer-consumer-completion-ordering",
      "semantic_obligation": "Any consumer of a shared buffer (collective, forward read, deallocation, bucket copy) must be ordered after the producing operation has completed across CUDA streams and async handles.",
      "relation_operator": "ordering",
      "semantic_objects": ["buffer-producing operation", "buffer-consuming operation", "shared buffer"],
      "schema_predicate": "FORALL (prod, cons, buf) in hazard_pairs: happens_before(complete(prod(buf)), start(cons(buf)))  -- includes: grad write before reduce-scatter, param all-gather before forward read, wgrad before all-reduce, init before DDP bucket copy, async send complete before dealloc/reuse",
      "permitted_topology_guards": ["DP>1", "PP>1", "DP>1 and PP>1"],
      "permitted_preconditions": ["non-default backward CUDA stream", "overlap-grad-reduce / overlap-param-gather", "interleaved 1F1B with async P2P and output deallocation", "DDP init on separate stream", "buffer reuse for fp8 param gather", "zero-bubble PP with FSDP manual accumulation"],
      "permitted_phases": ["init", "forward", "backward"],
      "positive_examples": ["D-021", "M-007", "M-019", "M-026", "M-031", "M-034", "M-NEW-46", "O-030"],
      "counterexamples": ["Overlapped communication correctly synchronized via stream waits/handle waits before consumption", "Deallocation of a buffer after its send handle has been waited on"],
      "merge_boundary": "Must not absorb D-022-style sort-key ordering: merging checkpoint shards in numeric order is a value-ordering (sort correctness) relation over file names, not a happens-before between concurrent operations - roles differ (§3.2) and no single race-witness signal covers both (§3.4).",
      "support_count": 8
    },
    {
      "template_id": "T11",
      "name": "checkpoint-restore-state-equality",
      "semantic_obligation": "After checkpoint load, every element of training state (parameters, optimizer moments, LR/scheduler fields, RNG, derived caches) must equal the value it had at save, keyed to the correct parameter.",
      "relation_operator": "restoration_after_reload",
      "semantic_objects": ["restored training state", "saved training state"],
      "schema_predicate": "FORALL k in state_keys: value_after_load(k) == value_at_save(k), with key mapping preserved under renames and derived caches (e.g. secondary param shards) refreshed to match restored primaries",
      "permitted_topology_guards": ["DP>1", "any"],
      "permitted_preconditions": ["ZeRO++ secondary tensors after gather/load", "ZeRO-3 universal checkpoint with multiple subgroups", "load_optimizer_state with changed block_group_size", "load_state_dict between forward and backward under FSDP FULL_SHARD", "initial_lr present in restored param_groups"],
      "permitted_phases": ["checkpoint_load", "checkpoint_save"],
      "positive_examples": ["D-024", "D-044", "M-017", "O-016", "O-026", "O-NEW-33"],
      "grounding_concerns": ["M-017: witness is loss deviation vs an uninterrupted reference run; the failing sub-state (optimizer vs RNG) is not isolated in the evidence"],
      "counterexamples": ["State intentionally overridden at load by an explicit user flag (that is T18's obligation, not a restore violation)", "Optimizer state legitimately re-initialized when the user requests a fresh optimizer"],
      "merge_boundary": "Must not absorb T13 dataloader reset: 'equal to saved value' and 'reset to defined initial value' are unrelated OR branches (§3.3).",
      "support_count": 6
    },
    {
      "template_id": "T12",
      "name": "checkpoint-save-completeness",
      "semantic_obligation": "A saved checkpoint must contain the complete expected inventory: every parameter key, every optimizer-state key, and one shard/file per rank or partition group of the active topology.",
      "relation_operator": "count_frequency_match",
      "semantic_objects": ["saved checkpoint artifact inventory", "expected live-state inventory"],
      "schema_predicate": "set(keys(saved_state_dict)) == set(keys(expected_state)) AND count(saved_shard_files) == expected_shard_count(topology) AND FORALL g in partition_groups: saved(g) exists",
      "permitted_topology_guards": ["TP>1", "DP>1", "single-rank"],
      "permitted_preconditions": ["MoE checkpoint save with TP>1", "MiCS partition groups", "FSDP use_orig_params + FULL_SHARD inner-module state_dict", "early-iteration save before optimizer state populated"],
      "permitted_phases": ["checkpoint_save"],
      "positive_examples": ["D-043", "D-046", "M-042", "O-033"],
      "counterexamples": ["Sharded checkpoints that deliberately store each param on exactly one owner rank per the format spec", "Optimizer state legitimately absent when save is configured model-only"],
      "merge_boundary": "Must not absorb T11: inventory/count diff at save time is a different observable than value equality after reload; a merged predicate would need unrelated branches (§3.3, §3.4).",
      "support_count": 4
    },
    {
      "template_id": "T13",
      "name": "dataloader-bookkeeping-reset",
      "semantic_obligation": "Data-loader bookkeeping (start index, iteration position) must be re-initialized to its defined starting value at run/epoch boundaries so the realized sample order matches the schedule.",
      "relation_operator": "restoration_after_reload",
      "semantic_objects": ["data-loader bookkeeping state", "run or epoch boundary"],
      "schema_predicate": "at each boundary b: loader_position(after b) == defined_initial(b)  -- start_index == 0 at a new epoch; bookkeeping reset between repeated runs so sample_order(run_i) == sample_order(run_j)",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["resume with restore_dataloader=False entering a new epoch", "LM evaluator run repeatedly within a session"],
      "permitted_phases": ["data_loading", "eval"],
      "positive_examples": ["O-006", "O-019"],
      "counterexamples": ["Mid-epoch resume that intentionally restores (not resets) the saved loader position", "Shuffled epochs where order differs by design across epochs"],
      "merge_boundary": "Must not absorb T11: reset-to-initial vs equal-to-saved are unrelated OR branches over different state roles (§3.3, §3.2).",
      "support_count": 2
    },
    {
      "template_id": "T14",
      "name": "scaling-scalar-finiteness",
      "semantic_obligation": "Every scalar that multiplies or divides losses/gradients (loss scale, clip coefficient, loss-normalization denominator) must be finite and its denominator nonzero at every use.",
      "relation_operator": "boundedness",
      "semantic_objects": ["loss/gradient scaling or normalization scalar"],
      "schema_predicate": "FORALL s in scaling_scalars(step): isfinite(s) AND (s used as denominator IMPLIES s > 0)",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["fp16 mixed precision loss scale input validation", "NaN/inf handling in gradient clipping", "instance masking where a batch can be fully masked"],
      "permitted_phases": ["forward", "backward", "optimizer_step"],
      "positive_examples": ["D-007", "D-038", "O-015"],
      "counterexamples": ["Dynamic loss scale legitimately halving after a genuine overflow", "Large-but-finite scale values within the configured range"],
      "merge_boundary": "Must not absorb T08: finiteness/positivity is a bound relation, not equality to a specified factor - different relation operator (§3.1).",
      "support_count": 3
    },
    {
      "template_id": "T15",
      "name": "counter-monotonic-progress",
      "semantic_obligation": "Cumulative training counters (optimizer step count, global FLOPs) must advance monotonically by their defined increment and never stall at zero or wrap through integer overflow.",
      "relation_operator": "monotonicity",
      "semantic_objects": ["cumulative training counter"],
      "schema_predicate": "FORALL steps t: counter(t+1) >= counter(t) AND counter(t+1) - counter(t) == defined_increment(t) (no stall at 0, no negative wrap)",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["SkipStepAdamW-style optimizers maintaining state['step']", "long runs where accumulated FLOPs exceed 32/64-bit ranges"],
      "permitted_phases": ["optimizer_step", "any"],
      "positive_examples": ["O-010", "O-025"],
      "counterexamples": ["Step counter intentionally not advanced on a legitimately skipped (overflow) step", "Counters reset at documented run boundaries"],
      "merge_boundary": "Must not absorb T22 parameter-update-effectiveness: the object is a scalar counter, not a parameter tensor, and the witness is counter arithmetic, not parameter checksums (§3.2).",
      "support_count": 2
    },
    {
      "template_id": "T16",
      "name": "positional-encoding-position-fidelity",
      "semantic_obligation": "The positional (rotary) encoding applied to each token must equal the spec encoding of that token's intended global position under the configured layout, including per-document resets and context-parallel shard mappings.",
      "relation_operator": "index_consistency",
      "semantic_objects": ["applied positional encoding", "token's intended global position"],
      "schema_predicate": "FORALL tokens t on rank r: applied_rope(t, r) == rope_spec(intended_position(t), layout_cfg)  -- intended_position honors supplied position_ids and CP shard-to-position mapping; layout_cfg honors interleaved vs half-half",
      "permitted_topology_guards": ["CP>1", "any"],
      "permitted_preconditions": ["--reset-position-ids with packed sequences", "context-parallel sequence chunking", "rotary_interleaved=True with yarn embeddings", "per-block CP-sharded RoPE buffers"],
      "permitted_phases": ["forward"],
      "positive_examples": ["M-011", "M-028", "M-037", "O-011"],
      "counterexamples": ["Contiguous 0..L-1 positions when no resets or sharding are configured", "Different-but-equivalent cached buffer layouts that produce identical per-token angles"],
      "merge_boundary": "Must not absorb M-027 sample-index integrity: dataset sample selection plays a different semantic role than token positional encoding and has a disjoint observable (§3.2).",
      "support_count": 4
    },
    {
      "template_id": "T17",
      "name": "loss-parameter-gradient-connectivity",
      "semantic_obligation": "Every trainable parameter on an active loss path must receive a nonzero gradient: no loss term may be detached from the autograd graph and no routing construction may make the gradient identically zero.",
      "relation_operator": "gradient_flow",
      "semantic_objects": ["trainable parameter", "loss computation graph"],
      "schema_predicate": "FORALL p in trainable_params_on_active_loss_path: grad_norm(p, step) > 0 (over a window of steps), AND requires_grad_graph(loss) reaches p",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["top-k=1 routing with post-topk softmax", "combined losses built from possibly-detached tensors (soft-CE branches)"],
      "permitted_phases": ["backward"],
      "positive_examples": ["M-014", "OC-NEW-9"],
      "counterexamples": ["Parameters intentionally frozen (requires_grad=False)", "Experts receiving zero gradient at a step because no tokens routed to them"],
      "merge_boundary": "Must not absorb T22: zero gradient at the loss side vs a nonzero gradient that never becomes a parameter update are witnessed by different signals (grad norm vs parameter delta) (§3.2, §3.4).",
      "support_count": 2
    },
    {
      "template_id": "T18",
      "name": "configured-value-effectiveness",
      "semantic_obligation": "The runtime-effective value of a user-configured setting (LR schedule overrides, softmax scale, optimizer-state layout mode) must equal the configured value once the configuration is accepted.",
      "relation_operator": "config_effectiveness",
      "semantic_objects": ["user-configured value", "runtime-effective value"],
      "schema_predicate": "FORALL knob k in accepted_config: effective(k, runtime) == configured(k)  -- e.g. lr_used(first_step_after_load) == scheduler(new_config), applied_softmax_scale == config.softmax_scale, checkpoint_optimizer_layout == layout(configured_optimizer_mode)",
      "permitted_topology_guards": ["any", "single-rank"],
      "permitted_preconditions": ["checkpoint resume with --override-opt_param-scheduler", "config.softmax_scale specified", "bf16 without ZeRO checkpoint layout"],
      "permitted_phases": ["checkpoint_load", "forward", "optimizer_step"],
      "positive_examples": ["M-015", "M-030", "M-NEW-8", "D-NEW-21"],
      "counterexamples": ["Checkpointed values legitimately taking precedence when no override flag is set", "Defaults applied where the user left the knob unset"],
      "merge_boundary": "Must not absorb T04: the reference here is the user's own configuration, not an external algorithm spec; merging would conflate oracle sources (§3.2). M-015 and M-030 describe the same override-ignored incident and count once (§4.1).",
      "support_count": 3
    },
    {
      "template_id": "T19",
      "name": "packed-sequence-attention-isolation",
      "semantic_obligation": "When multiple documents are packed into one sequence, attention mass across document boundaries must be exactly zero (block-diagonal mask honored by the backend).",
      "relation_operator": "exclusivity",
      "semantic_objects": ["attention mass", "packed-document boundaries"],
      "schema_predicate": "FORALL token pairs (i, j) with doc(i) != doc(j): attention(i, j) == 0; equivalently output(doc_d) invariant to co-packed documents",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["transformer_engine backend with block-diagonal attention_mask", "cu_doc_lens provided with sequence packing"],
      "permitted_phases": ["forward"],
      "positive_examples": ["M-036", "OC-NEW-47"],
      "counterexamples": ["Ordinary causal attention within a single document", "Training modes that intentionally allow cross-document context (no packing isolation configured)"],
      "merge_boundary": "Must not absorb T20 padding-loss-exclusion: excluded attention mass and excluded loss contributions are different objects with different witnesses (§3.2, §3.4).",
      "support_count": 2
    },
    {
      "template_id": "T20",
      "name": "padding-loss-exclusion",
      "semantic_obligation": "Padding (ignore-index) token positions must contribute zero to the training loss; the loss mask must exclude exactly the padding positions.",
      "relation_operator": "exclusivity",
      "semantic_objects": ["loss contributions", "padding token positions"],
      "schema_predicate": "FORALL positions t with token(t) == pad: loss_mask(t) == 0; equivalently count(loss_mask == 0) == count(padding_tokens) per batch",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["cross-entropy API with ignore_index parameter", "padded fixed-sequence-length datasets"],
      "permitted_phases": ["forward", "data_loading"],
      "positive_examples": ["O-007", "O-023"],
      "counterexamples": ["Sequences without padding, where all positions legitimately contribute", "Masking schemes that intentionally include special tokens per config"],
      "merge_boundary": "Must not absorb T19: loss-mask exclusion is witnessed on the loss/mask tensors, not on attention mass; roles and signals differ (§3.2, §3.4). O-023's raw count_frequency_match coding is an equivalent restatement of the same exclusion relation.",
      "support_count": 2
    },
    {
      "template_id": "T21",
      "name": "overflow-skip-step-validity",
      "semantic_obligation": "An optimizer step is skipped if and only if a genuine gradient overflow (non-finite gradient) occurred at that step; skip frequency must match overflow frequency.",
      "relation_operator": "count_frequency_match",
      "semantic_objects": ["step-skip events", "genuine gradient-overflow events"],
      "schema_predicate": "FORALL steps t: skipped(t) <=> EXISTS g in grads(t): !isfinite(g); over a window: count(skipped) == count(overflow_events)",
      "permitted_topology_guards": ["DP>1", "any"],
      "permitted_preconditions": ["fp16 fused optimizer overflow guard", "fp16 loss scaler with overlapped grad-reduce/param-gather handle initialization"],
      "permitted_phases": ["optimizer_step"],
      "positive_examples": ["D-015", "M-008"],
      "counterexamples": ["Steps correctly skipped during genuine overflow bursts early in fp16 training", "Loss-scale warmup skips consistent with the dynamic scaling policy"],
      "merge_boundary": "Must not absorb T22: 'skip iff overflow' and 'non-skipped step must update parameters' would be unrelated OR branches in one predicate (§3.3).",
      "support_count": 2
    },
    {
      "template_id": "T22",
      "name": "parameter-update-effectiveness",
      "semantic_obligation": "A non-skipped optimizer step with finite nonzero gradients must actually change each trainable parameter, and the tensor consumed by forward must be the optimizer-registered object.",
      "relation_operator": "update_effectiveness",
      "semantic_objects": ["trainable parameter", "optimizer update application"],
      "schema_predicate": "NOT skipped(t) AND grad_norm(p, t) > 0 IMPLIES checksum(p, t+1) != checksum(p, t); AND id(forward_used_tensor(p)) == id(optimizer_registered_param(p))",
      "permitted_topology_guards": ["single-rank", "any"],
      "permitted_preconditions": ["tensors with >= 2^31 elements (size metadata width)", "MoE gate under ZeRO with fp16/bf16 downcast rebinding"],
      "permitted_phases": ["optimizer_step", "forward"],
      "positive_examples": ["D-006", "D-NEW-81"],
      "counterexamples": ["Frozen parameters that must NOT change (see singleton D-NEW-43 for the converse obligation)", "Parameters unchanged on steps legitimately skipped by the loss scaler"],
      "merge_boundary": "Must not absorb T21: update-effectiveness is conditioned on the step not being skipped; folding skip-validity in would require an unrelated OR branch (§3.3).",
      "support_count": 2
    },
    {
      "template_id": "T23",
      "name": "unrelated-operation-state-preservation",
      "semantic_obligation": "A stateful object (residual-stream input, module identity attribute, global RNG state) must retain its value across operations that are not designated writers of that state.",
      "relation_operator": "state_preservation",
      "semantic_objects": ["stateful object", "operation not designated to modify it"],
      "schema_predicate": "FORALL (x, op) with op NOT IN designated_writers(x): state(x, after op) == state(x, before op)  -- e.g. checksum(block_input) before norm == residual_addend; expert_id after hook registration == expert_id before; global_rng_state after config build == before",
      "permitted_topology_guards": ["single-rank", "EP>1", "any"],
      "permitted_preconditions": ["pre-LN sequential block (norm must not clobber the residual variable)", "ZeRO-3 hook registration over modules with custom id attributes", "dataset-config build in a process relying on global RNG determinism"],
      "permitted_phases": ["forward", "init", "data_loading"],
      "positive_examples": ["B13", "O-002", "D-009", "O-022"],
      "counterexamples": ["In-place updates performed by the state's designated owner (e.g. optimizer writing parameters)", "Documented in-place activation ops whose consumers all expect the mutated value"],
      "merge_boundary": "Must not absorb T11: clobbering by a non-designated writer during live execution is a different relation than restore-equality across a save/load boundary (§3.2). B13 and O-002 describe the same residual-clobber incident and count once (§4.1).",
      "support_count": 3
    },
    {
      "template_id": "T24",
      "name": "intra-run-recomputation-determinism",
      "semantic_obligation": "Re-executions of the same logical computation within one step (activation-checkpoint recompute passes, repeated per-subgroup metadata updates) must produce values identical to the original execution.",
      "relation_operator": "determinism",
      "semantic_objects": ["re-executed computation output", "original execution output"],
      "schema_predicate": "FORALL re-executions e of computation c within step t: value(c, e) == value(c, original)  -- dropout masks under recompute equal the forward masks; bias-correction terms bit-identical across subgroup invocations",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["activation checkpointing with dropout (and torch.compile)", "ZeRO-3 CPU-offloaded Adam with multiple subgroups per step"],
      "permitted_phases": ["backward", "optimizer_step"],
      "positive_examples": ["D-005", "D-023", "D-008"],
      "counterexamples": ["Runs without recompute where each computation executes once", "Numerically-equal-but-differently-computed paths explicitly documented as tolerance-equivalent"],
      "merge_boundary": "Must not absorb T03: the witness here is a self-comparison inside one run (no reference run/config needed) - a different observable-signal class (§3.4). D-005 and D-023 describe the same CPUAdam subgroup incident and count once (§4.1).",
      "support_count": 2
    },
    {
      "template_id": "T25",
      "name": "sharded-layout-conformance",
      "semantic_obligation": "The layout, shape, and distribution kind of every tensor an operation touches must equal the layout mandated by the active parallel configuration (correct shard slice per rank, CP-adjusted p2p shapes, local vs distributed tensor kind).",
      "relation_operator": "sharding_layout_consistency",
      "semantic_objects": ["tensor layout/shape used by an operation", "layout mandated by the parallel configuration"],
      "schema_predicate": "FORALL (t, op): layout(t, op) == expected_layout(t, parallel_cfg)  -- shape(p2p_buffer) == shape(activation)/cp_size on the sequence dim; rank r consumes slice_r(gate) not full gate; loss tensors combined with the declared local/distributed kind",
      "permitted_topology_guards": ["TP>1", "PP>1 and CP>1"],
      "permitted_preconditions": ["pipeline schedules with context parallelism", "MoE gate with kv_head < tp_size", "fused-linear loss under TP with FSDP wrappers"],
      "permitted_phases": ["forward"],
      "positive_examples": ["M-023", "M-039", "O-012"],
      "counterexamples": ["Replicated tensors correctly used whole on every rank", "Shapes that legitimately differ across stages per the declared schedule"],
      "merge_boundary": "Must not absorb T16: layout of communicated/sharded tensors is a different object role than the token-position index mapping of positional encodings (§3.2).",
      "support_count": 3
    },
    {
      "template_id": "T26",
      "name": "exactly-once-side-effect",
      "semantic_obligation": "A per-boundary side effect (aux-loss tracker increment per training step, final-checkpoint save per step) must occur exactly once per boundary, including under recompute passes and restarts.",
      "relation_operator": "count_frequency_match",
      "semantic_objects": ["side-effect event", "step/run boundary"],
      "schema_predicate": "FORALL boundaries b: count(event, b) == 1  -- tracker_increments(step) == 1 even when recompute reruns the module; saves(final_step) == 1 across restarts and never overwrite an existing valid artifact",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["activation recompute re-running router forward", "training resumed from checkpoint reaching the final step again"],
      "permitted_phases": ["forward", "checkpoint_save"],
      "positive_examples": ["M-NEW-21", "O-039"],
      "counterexamples": ["Multiple forward passes per step that are all designated to log (documented per-micro-batch logging)", "Periodic checkpoints legitimately saved at every interval boundary"],
      "merge_boundary": "Must not absorb T21: exactly-once boundary counting has no overflow condition; merging with skip-iff-overflow would create unrelated OR branches (§3.3).",
      "support_count": 2
    },
    {
      "template_id": "T27",
      "name": "init-distribution-conformance",
      "semantic_obligation": "Parameter statistics immediately after initialization must conform to the configured initialization spec: correct (depth-scaled) standard deviation, never all-zero unless specified, never uninitialized memory.",
      "relation_operator": "value_scaling_consistency",
      "semantic_objects": ["parameter statistics at initialization", "configured initialization spec"],
      "schema_predicate": "FORALL W in initialized_params: |std(W) - spec_std(W, depth, cfg)| <= tol AND NOT (spec_std(W) > 0 AND std(W) == 0) AND stats(W) inconsistent_with_uninitialized_memory == false",
      "permitted_topology_guards": ["any"],
      "permitted_preconditions": ["depth-scaled output-projection init configured", "muP enabled (readout zero-init interactions)", "optional projections materialized from meta device"],
      "permitted_phases": ["init"],
      "positive_examples": ["O-024", "O-NEW-21", "OC-NEW-66"],
      "counterexamples": ["Weights specified as zero-init by the architecture (e.g. residual output scaling schemes that document zero init)", "Statistical fluctuation of small tensors within tolerance of spec std"],
      "merge_boundary": "Must not absorb T08: the object is init-time weight statistics, not a runtime scaled quantity, and the witness is a one-shot statistical test at init (§3.2, §3.4).",
      "support_count": 3
    }
  ],
  "singletons": [
    {"case_id": "D-022", "relation_operator": "ordering", "why_not_promoted": "only 1 independent case (checkpoint shard files must merge in numeric, not lexicographic, order - sort-key correctness, not a happens-before race, so not expressible in T10 plus guards)"},
    {"case_id": "D-027", "relation_operator": "conservation", "why_not_promoted": "only 1 independent case (softmax coefficients must sum to one over the designated dimension; objects differ from T07's gradient accumulation per §3.2)"},
    {"case_id": "D-041", "relation_operator": "structural_integrity", "why_not_promoted": "only 1 independent case (checkpoint directory step label must equal internal global_step - label/content consistency, not inventory completeness T12 nor restore equality T11)"},
    {"case_id": "D-NEW-43", "relation_operator": "exclusivity", "why_not_promoted": "only 1 independent case (frozen requires_grad=False parameters must be excluded from trainable partitions and optimizer state; converse of T22 but a distinct exclusion relation)"},
    {"case_id": "M-025", "relation_operator": "dtype_preservation", "why_not_promoted": "only 1 independent case (master weights must be created directly in params_dtype so init values equal source elementwise; the runtime dtype tag ends up correct, so T05's signal cannot witness it - §3.4)"},
    {"case_id": "M-027", "relation_operator": "index_consistency", "why_not_promoted": "only 1 independent case (stored sample index must equal source document index without int32 truncation; data-sample selection role differs from T16's positional encoding per §3.2)"},
    {"case_id": "O-001", "relation_operator": "structural_integrity", "why_not_promoted": "only 1 independent case (attention and FFN paths must use distinct normalization modules; parameter-aliasing distinctness, not state clobbering T23)"},
    {"case_id": "O-020", "relation_operator": "sampling_uniformity", "why_not_promoted": "only 1 independent case (downsampling must draw a random subset, not the file prefix; randomness has no shared observable signal with O-021's ratio counts - §3.4)"},
    {"case_id": "O-021", "relation_operator": "count_frequency_match", "why_not_promoted": "only 1 independent case (realized per-source sample counts must match configured mixture ratios without duplicates; kept separate from O-020 per §3.4)"}
  ],
  "not_codable": []
}

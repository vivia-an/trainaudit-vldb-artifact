#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: B3 (deepspeed)
# Title: DeepSpeedEngine.communication_data_type defaults to fp32 when only bfloat16 is enabled
# Generated against: microsoft/DeepSpeed @ buggy=d56268f3~1 fixed=d56268f3
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [B3] BUG DETECTED: <rule_id>: <message>
#   [B3] CLEAN: <message>
#   [B3] FAIL: <stage>: <error>
set -euo pipefail
DS_DIR="${DS_DIR:?Set DS_DIR to deepspeed repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="d56268f3~1"
FIXED_COMMIT="d56268f3"

cd "$DS_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$DS_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on B3 $label ($rev) ====="
    cd "$DS_DIR"
    git checkout -q "$rev"
    
    set +e
    torchrun --nproc_per_node=1 --master_port=$((29550 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

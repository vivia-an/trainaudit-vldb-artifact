#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: M-010 (megatron-lm)
# Title: MoE aux_loss accumulated twice with activation checkpointing
# Generated against:  @ buggy=892c4c85448c6f81ee8bf8347c430ecba528d634 fixed=e6d56d6828c0773f55772b92b2ec0eed5639665e
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [M-010] BUG DETECTED: <rule_id>: <message>
#   [M-010] CLEAN: <message>
#   [M-010] FAIL: <stage>: <error>
set -euo pipefail
MEGATRON_DIR="${MEGATRON_DIR:?Set MEGATRON_DIR to megatron-lm repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="892c4c85448c6f81ee8bf8347c430ecba528d634"
FIXED_COMMIT="e6d56d6828c0773f55772b92b2ec0eed5639665e"

cd "$MEGATRON_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$MEGATRON_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on M-010 $label ($rev) ====="
    cd "$MEGATRON_DIR"
    git checkout -q "$rev"
    export PYTHONPATH="$SCRIPT_DIR:$MEGATRON_DIR:${PYTHONPATH:-}"
    export CUDA_DEVICE_MAX_CONNECTIONS=1
    rm -f tools/__init__.py 2>/dev/null || true
    [ -f tools/__init__.py ] || touch tools/__init__.py
    set +e
    torchrun --nproc_per_node=2 --master_port=$((29532 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

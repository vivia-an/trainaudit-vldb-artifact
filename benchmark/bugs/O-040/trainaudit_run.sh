#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: O-040 (olmo-core)
# Title: A100 peak flops specification 2x too low
# Generated against: allenai/OLMo-core @ buggy=f49e7559 fixed=db2b6eff
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [O-040] BUG DETECTED: <rule_id>: <message>
#   [O-040] CLEAN: <message>
#   [O-040] FAIL: <stage>: <error>
set -euo pipefail
OLMOCORE_DIR="${OLMOCORE_DIR:?Set OLMOCORE_DIR to olmo-core repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="f49e7559"
FIXED_COMMIT="db2b6eff"

cd "$OLMOCORE_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$OLMOCORE_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on O-040 $label ($rev) ====="
    cd "$OLMOCORE_DIR"
    git checkout -fq "$rev"
    find . -name __pycache__ -type d -prune -exec rm -rf {} + 2>/dev/null || true
    find . -name "*.pyc" -delete 2>/dev/null || true

    set +e
    torchrun --nproc_per_node=1 --master_port=$((29636 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: O-NEW-5 (olmo)
# Title: Embedding init std missing sqrt(d_model) factor — embeddings 16x too small
# Generated against: allenai/OLMo @ buggy=ce5f30d5~1 fixed=ce5f30d5
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [O-NEW-5] BUG DETECTED: <rule_id>: <message>
#   [O-NEW-5] CLEAN: <message>
#   [O-NEW-5] FAIL: <stage>: <error>
set -euo pipefail
OLMO_DIR="${OLMO_DIR:?Set OLMO_DIR to olmo repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="ce5f30d5~1"
FIXED_COMMIT="ce5f30d5"

cd "$OLMO_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$OLMO_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on O-NEW-5 $label ($rev) ====="
    cd "$OLMO_DIR"
    git checkout -fq "$rev"
    find . -name __pycache__ -type d -prune -exec rm -rf {} + 2>/dev/null || true
    find . -name "*.pyc" -delete 2>/dev/null || true

    set +e
    torchrun --nproc_per_node=1 --master_port=$((29607 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

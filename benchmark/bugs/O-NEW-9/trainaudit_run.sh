#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: O-NEW-9 (olmo)
# Title: Token IDs silently truncated to 16-bit by modulo 2^16
# Generated against: allenai/OLMo @ buggy=167a7ac8~1 fixed=167a7ac8
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [O-NEW-9] BUG DETECTED: <rule_id>: <message>
#   [O-NEW-9] CLEAN: <message>
#   [O-NEW-9] FAIL: <stage>: <error>
set -euo pipefail
OLMO_DIR="${OLMO_DIR:?Set OLMO_DIR to olmo repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="167a7ac8~1"
FIXED_COMMIT="167a7ac8"

cd "$OLMO_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$OLMO_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on O-NEW-9 $label ($rev) ====="
    cd "$OLMO_DIR"
    git checkout -q "$rev"
    
    set +e
    torchrun --nproc_per_node=1 --master_port=$((29659 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

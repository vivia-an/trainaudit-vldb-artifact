#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: B8 (deepspeed)
# Title: InferenceEngine creates EP groups with num_experts instead of ep_size
# Generated against: microsoft/DeepSpeed @ buggy=8cbbbb53~1 fixed=8cbbbb53
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [B8] BUG DETECTED: <rule_id>: <message>
#   [B8] CLEAN: <message>
#   [B8] FAIL: <stage>: <error>
set -euo pipefail
DS_DIR="${DS_DIR:?Set DS_DIR to deepspeed repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="8cbbbb53~1"
FIXED_COMMIT="8cbbbb53"

cd "$DS_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$DS_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on B8 $label ($rev) ====="
    cd "$DS_DIR"
    git checkout -q "$rev"
    
    set +e
    torchrun --nproc_per_node=1 --master_port=$((29551 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"

#!/bin/bash
# AUTO-GENERATED by benchmark/eval/gen_driver.py — smoke-pending.
# Bug: OC-NEW-22 (olmo-core)
# Title: RoPE positions use global token index in packed mode — second document gets [k_doc1_len, ...] instea
# Generated against: allenai/OLMo-core @ buggy=df72f4ee~1 fixed=df72f4ee
# Contract output (to be parsed by benchmark/eval/run_all.py):
#   [OC-NEW-22] BUG DETECTED: <rule_id>: <message>
#   [OC-NEW-22] CLEAN: <message>
#   [OC-NEW-22] FAIL: <stage>: <error>
set -euo pipefail
OLMOCORE_DIR="${OLMOCORE_DIR:?Set OLMOCORE_DIR to olmo-core repo root}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

BUGGY_COMMIT="df72f4ee~1"
FIXED_COMMIT="df72f4ee"

cd "$OLMOCORE_DIR"
ORIG=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || git rev-parse HEAD)
trap 'cd "$OLMOCORE_DIR" && git checkout -q "$ORIG" 2>/dev/null || true' EXIT

run_one() {
    local label="$1" rev="$2"
    echo "===== TrainAudit on OC-NEW-22 $label ($rev) ====="
    cd "$OLMOCORE_DIR"
    git reset --hard "$rev" 2>&1 | tail -2
    git clean -fdq -- src/ 2>/dev/null || true
    find . -name __pycache__ -type d -prune -exec rm -rf {} + 2>/dev/null || true
    find . -name "*.pyc" -delete 2>/dev/null || true

    set +e
    torchrun --nproc_per_node=1 --master_port=$((29609 + RANDOM % 100))  "$SCRIPT_DIR/trainaudit_driver.py" 2>&1
    set -e
    echo
}

run_one "BUGGY" "$BUGGY_COMMIT"
run_one "FIXED" "$FIXED_COMMIT"
